FROM       timn/fedora-ros:f23-jade-base
MAINTAINER Tim Niemueller <niemueller@kbsg.rwth-aachen.de> 

RUN /bin/bash -c "source /etc/profile; \
  mkdir -p /opt/ros/catkin_ws_${ROS_DISTRO}_robot/src; \
  cd /opt/ros/catkin_ws_${ROS_DISTRO}_robot; \
  rosinstall_generator robot --rosdistro $ROS_DISTRO --deps --wet-only --tar --exclude RPP > $ROS_DISTRO-robot.rosinstall; \
  wstool init -j $(nproc) src ${ROS_DISTRO}-robot.rosinstall; \
  rosdep install --from-paths src --ignore-src --rosdistro $ROS_DISTRO -y; \
  catkin_make_isolated --install --install-space /opt/ros/$ROS_DISTRO \
    -DCMAKE_BUILD_TYPE=$ROS_BUILD_TYPE; \
  rm -rf *_isolated; \
  "
